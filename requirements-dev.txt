# Development / CI dependencies for twingate-log-shipper — pinned for reproducible CI.
# Runtime deps live in requirements.txt (the only ones shipped in the Docker image).
# These pin the lint/type/test toolchain so CI doesn't drift when a tool releases a new
# version. Bump deliberately and re-run the suite. CI installs this lockfile (see ci.yml).
# Install (dev): pip install -r requirements.txt -r requirements-dev.txt && pip install -e . --no-deps

pytest==9.0.2
pytest-asyncio==1.3.0
moto[s3,server]==5.1.22
boto3==1.42.70            # sync S3 client used in test_integration.py to verify uploads
werkzeug==3.1.7           # moto server mode (Flask WSGI runner) for the integration test
jsonschema==4.24.1
types-jsonschema==4.26.0.20260325
ruff==0.15.8
mypy==1.19.1
pip-audit==2.10.0         # dependency vulnerability scan (CI gate against requirements.txt)
